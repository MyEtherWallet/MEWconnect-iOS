{% include 'header' %}

@import XCTest;
@import OCMock;

#import "{{ prefix }}{{ module_info.name }}Router.h"

@interface {{ prefix }}{{ module_info.name }}RouterTests : XCTestCase

@property (nonatomic, strong) {{ prefix }}{{ module_info.name }}Router *router;

@end

@implementation {{ prefix }}{{ module_info.name }}RouterTests

#pragma mark - Config the environment

- (void)setUp {
    [super setUp];

    self.router = [[{{ prefix }}{{ module_info.name }}Router alloc] init];
}

- (void)tearDown {
    self.router = nil;

    [super tearDown];
}

@end
